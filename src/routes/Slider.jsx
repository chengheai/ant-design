import React from 'react';
import { connect } from 'dva';


class SliderC extends React.Component {

  render(){

    return (
      
      <div>
        Slider
      </div>
    );
  }
  
}

SliderC.propTypes = {
};

export default connect()(SliderC);
